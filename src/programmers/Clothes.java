package programmers;

class Clothes {
    public int solution(int n, int[] lost, int[] reserve) {
        int answer = 0;
        return answer;
    }
}
// 2 <= n <= 30
// 1 <= lost <= n (no overlap)
// 1 <= reserve <= n (no overlap)
// reserve만 대여가능
// reserve도 도난 가능 (단, reserve는 2개의 체육복 중 하나만 도난)

