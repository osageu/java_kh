package com.kh.chap01.condition;

import java.util.Scanner;

public class A_If {
	Scanner osageu = new Scanner(System.in);
	/*
	 * 프로그램 진행은 기본적으로 순차적으로 진행됨
	 * 단, 이 순차적인 흐름을 바꾸고자 한다면 "제어문"이라는 걸 통해서 직접 제어가능
	 * 
	 * 선택적으로 실행 ==> 조건문
	 * 반복적으로 실행 ==> 반복문
	 * 그 외의 흐름 제어 ==> 분기문
	 * 
	 * <조건문>
	 * 조건식을 통해 참이냐 거짓이냐를 판단해서 해당조건이 참일 경우 그에 해당하는 실행문을 실행
	 * 조건식의 결과 true / false 여야됨
	 * 조건식에서는 비교연산자(대소, 동등), 논리연산자(&&, ||), 논리부정연산자(!)
	 * 
	 * 조건문은 if문과 switch문으로 나뉨
	 * 
	 * 그 중 if문은 크게 3가지로 나뉨
	 * 1. 단독 if문
	 * 2. if-else문
	 * 3. if-else if문
	 * 
	 * 1. 단독 if문
	 * [표현법]	if(조건식) {
	 * 					실행할 코드...;
	 * 				}
	 * --> 조건식이 true일 경우, 중괄호 안의 코드 실행
	 * --> 조건식이 false일 경우, 중괄호 안의 코드 무시하고 건너뜀
	 * 
	 */
	public void method1() {
		// 실습문제 1번
		
	}
}
